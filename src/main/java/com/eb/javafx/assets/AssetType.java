package com.eb.javafx.assets;

/** Generic asset categories that keep concrete authored files application-owned. */
public enum AssetType {
    IMAGE,
    AUDIO,
    FONT,
    SCENE_DATA,
    DISPLAY_DATA,
    CONFIG,
    OTHER
}
