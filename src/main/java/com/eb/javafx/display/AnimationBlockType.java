package com.eb.javafx.display;

/** Category of an animation block: continuous loop, mouth-sync talking, or one-shot event response. */
public enum AnimationBlockType {
    IDLE,
    TALKING,
    EVENT
}
