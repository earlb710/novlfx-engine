package com.eb.javafx.settings;

/** Generic setting value type used by reusable settings screens and persistence adapters. */
public enum SettingType {
    BOOLEAN,
    INTEGER,
    DECIMAL,
    TEXT
}
